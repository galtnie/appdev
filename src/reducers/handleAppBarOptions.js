const initialState = [
  "Services",
  "Technologies",
  "Portfolio",
  "Company",
  "Blog",
  "Contact us",
];

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
